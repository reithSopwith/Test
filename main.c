//In the program I will be playing with some operators
#include <stdio.h>
int main() {
    int Length = 15;
    int Breadth = 10;
    int Area = Length * Breadth;
    printf("The area of the rectangle is %d\n", Area);
}